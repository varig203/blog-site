// seperate file just to call a script from any page easily. SEO isn't the biggest concern atp.

import { renderHeader, renderFooter } from './header.js';

renderHeader('header-wrapper');
renderFooter('footer-wrapper');